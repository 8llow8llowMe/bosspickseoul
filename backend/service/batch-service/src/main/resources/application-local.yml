spring:
  batch:
    jdbc:
      initialize-schema: never
    job:
      enabled: false

app:
  batch:
    area-boundary-seed:
      enabled: false
      job-name: areaBoundarySeedJob
      source-dir: ""
