---
project: nowdoboss
cwd: /Users/seonghoho/Documents/projects/nowdoboss
branch: 로컬 develop e00fb51 (워크트리 .worktrees/bosspick-develop) / 원격 origin/develop a8f8600 — 불일치
timestamp: 2026-08-27T15:21:05+09:00
title: 원격 develop 롤백 발견(#144~#146 소실) → 복구 후 시뮬레이션 UI 3건·B3
files: []
---

## 작업 주제: 원격 develop 롤백 발견(#144~#146 소실) → 복구 후 시뮬레이션 UI 3건·B3

### 요약

BossPickSeoul FE. **이 세션은 코드를 한 줄도 고치지 않았다.** 직전 핸드오프
(`2026-08-27-151330--ui-------b3.md`, 15:13)를 쓴 직후 새 대화가 열려 곧바로 `/context-handoff` 가
호출됐고, 상태 수집 과정에서 **원격 `develop` 이 뒤로 롤백돼 있는 것을 발견했다.**

**핵심 발견 — 머지된 FE PR 3개가 원격 develop 에서 사라졌다.**

- `#144`(시뮬레이션 저장·이력 B2) · `#145`(로그인 복귀) · `#146`(토스트+이어하기)는 GitHub 상
  **MERGED 상태이고 머지 커밋 oid 도 남아 있다**(4405ffc / 6e62491 / e00fb51).
- 그런데 **원격 `refs/heads/develop` 은 `a8f8600`** — 세 머지의 직전 커밋(BE CORS 정리)이다.
- PushEvent 기록:
  `05:35:58 seonghoho a8f8600→4405ffc` · `05:36:12 seonghoho 4405ffc→6e62491` ·
  **`05:43:03 shindonggeun e00fb51→a8f8600`** (=14:43 KST, 강제 푸시 되돌림)
- `branch protection = false` 라 되돌림이 막히지 않았다.
- 세 머지 커밋은 **원격 어느 ref 에서도 도달 불가**(브랜치 3개도 머지 후 삭제됨).
  **로컬 `.worktrees/bosspick-develop` 의 develop(e00fb51)에만 남아 있다.**

즉 **작업물은 유실되지 않았지만, 원격/배포에는 없다.** 직전 핸드오프의
"머지 완료 / 열린 PR 없음 / develop HEAD 가 배포됐으니 Jenkins 미배포 의심은 무효" 라는 서술은
**이 롤백 이후 기준으로는 더 이상 참이 아니다.**

### 내린 결정

- **이 세션에서는 복구를 실행하지 않았다.** develop 을 원격에 되돌려 놓는 것은
  팀 공유 브랜치를 바꾸는 되돌리기 어려운 행위이고, `shindonggeun` 이 **의도적으로** 되돌린 것일
  가능성(BE 쪽 문제로 FE 머지를 잠시 뺐다든지)을 배제할 수 없다. → **사람 확인 후 실행.**
- 다음 세션의 첫 작업은 UI 폴리시가 아니라 **develop 정합성 복구**다. 이걸 먼저 하지 않으면
  `origin/develop` 에서 딴 새 브랜치가 #144~#146 을 통째로 빼먹은 베이스 위에 올라간다.
- 직전 핸드오프에 적힌 워크트리 생성 명령(`... -b feature/fe/simulation-ui-polish origin/develop`)은
  **지금 그대로 쓰면 안 된다.** `origin/develop` 이 a8f8600 이라 조용히 3개 PR 이 빠진다.
  복구 전에는 **로컬 `develop`** 을 베이스로 써야 한다.

**직전 세션에서 확정된 기술 결정은 그대로 유효하다** (재론하지 않는다. 원문은
`2026-08-27-151330--ui-------b3.md` 참조):

- 아이디 타입 비대칭 — 응답 String / 요청 integer, `franchiseeId` 계열은 API 클라이언트 경계에서
  number 정규화(`lib/api/simulation.ts`), `historyId` 는 string 유지.
- 조건 URL 코덱 두 벌 — 요청 코덱에 표시 전용 `brandName` 을 넣지 않는다.
- 전역 토스트(`@/components/ui/toast` + `@/lib/ui/toast-state`, `AppProviders` 안 `ToastProvider`).
  문서 흐름에 피드백을 넣지 않는다.
- 로그인 복귀 경로 `@/lib/auth/return-path` 한 벌. 카카오는 `auth_return` 쿠키.

### 남은 작업

#### 0. (선행) 원격 develop 복구 — 사람 확인 필수

1. **먼저 `shindonggeun` 에게 확인한다**: 14:43 의 develop 되돌림이 의도된 것인지, 이유가 뭔지.
   의도된 롤백이면 FE 3개를 언제 어떻게 다시 올릴지 합의부터 한다.
2. 되돌림이 실수였다면 복구는 **fast-forward 푸시 한 방**이다(force 아님 — a8f8600 은 e00fb51 의
   조상이다):
   ```bash
   git -C /Users/seonghoho/Documents/projects/nowdoboss/.worktrees/bosspick-develop push origin develop
   ```
   푸시 전 `git log --oneline origin/develop..develop` 이 정확히 3커밋(4405ffc·6e62491·e00fb51)인지
   확인할 것.
3. 푸시 후 **Jenkins 배포를 실제로 확인한다.** 직전 핸드오프의 "배포 확인됨" 근거가 사라졌으므로
   `frontend-web` 라벨 게이트(fail-closed)를 통과한 빌드가 도는지 다시 봐야 한다.
4. 원격이 또 뒤로 갈 수 있으니, 새 브랜치를 딸 때마다 `git fetch` 후
   **`origin/develop` 이 로컬 develop 과 같은지 먼저 확인**하는 습관을 붙인다.
   (branch protection 이 꺼져 있어 구조적으로 막혀 있지 않다.)

#### 1. 시뮬레이션 **결과 화면** 폭 1320px → 800px

- `frontend/src/components/simulation/report/simulation-report-page.tsx:43`
  `width: min(1320px, calc(100% - 40px))` → `min(800px, ...)`
- **입력 화면(`simulation-builder-page.tsx`)은 건드리지 않는다** — `minmax(0,1fr) 380px` 2단이라
  800px 로 줄이면 sticky 결과 패널이 깨진다. 사용자가 말한 "결과화면"은 `/simulation/report`.
- 640px 이하 분기(`width: calc(100% - 32px)`)는 그대로.

#### 2. 비용 구성 **도넛 영역 400px** — 착수 전 문구 확인 필요

- `frontend/src/components/simulation/report/simulation-cost-breakdown.tsx:33`
  `Layout` 의 `grid-template-columns: 220px minmax(0, 1fr)` 에서 220px → 400px.
- **사용자 원문이 모호하다**("도넛 그래프 표시하는 것도 400px로 클리프? 수정하고싶어").
  (a) 도넛이 놓인 **컬럼 폭** 400px(가장 유력) vs (b) 도넛 **자체 크기**(현재 `height={180}`).
- **(b)라면**: `DonutChart`(`src/components/analysis/charts/donut-chart.tsx:120`)는 **공용**이다
  (analysis-result-view · status-detail · simulation-cost-breakdown · simulation-customer-insight).
  → `height` 를 **옵셔널 prop 으로 추가**하고 시뮬레이션 호출부만 넘긴다(기본 180 유지).
- **1번과 상호작용**: 컨테이너 800px + 도넛 컬럼 400px 이면 비용 표가 ~360px 로 좁아진다.
  767px 이하 1단 분기와 함께 실화면 확인.

#### 3. 헤더에 `상권 시뮬레이션` 추가

- `frontend/src/components/layout/site-header.tsx:368` 의 `navigationItems` 에
  `{ href: '/simulation', label: '상권 시뮬레이션' }` 추가.
- **활성 판정 함정**: 396행 `pathname === href || pathname.startsWith(`${href}/`)` →
  `/simulation/report`·`/simulation/compare` 는 활성, **`/analysis/simulation` 은 비활성**
  (그쪽은 `/analysis` 가 활성). 의도인지 확인하고 필요하면 예외 분기(389·393행 예시).
- **레이블 길이**: 기존 3개는 4자, `상권 시뮬레이션` 은 8자. 헤더가 PR #114 로 햄버거 전용 +
  플로팅 우측 패널로 개편됐으니 태블릿/모바일 줄바꿈·넘침 확인 필수. 짧은 후보 `시뮬레이션` —
  사용자 확인.
- `INNER_WIDTH` 매핑(41행): `/simulation` 을 `default`(1120px)로 둘지, 본문 800px 과 어울리는지 확인.

#### 4. 슬라이스 B3 — A/B 비교 (UI 3건 뒤, 별도 브랜치)

- 정본: `frontend/docs/superpowers/plans/2026-08-27-simulation-report-slice-b.md` **Task 10~13**
- 범위: `a.`/`b.` 접두사 쌍 코덱 · 미러 막대 · 컴팩트 조건 편집기 · 비교 화면 ·
  `compare` 라우트 placeholder 2개 교체 · 리포트에 `비교에 추가` CTA
- 선행 조건 없음(아이디 타입 비대칭은 #144 에서 해소 — 단, 그 커밋이 원격에 없다는 점은 0번 참조).
- 계획에 박힌 규칙 그대로:
  - 비교 API 없음 → `createSimulationReportPair` 로 `POST /simulations/reports` **2회 병렬**
  - **부분 성공 금지**(G10) — `Promise.all` 을 `allSettled` 로 바꾸지 않는다. 오류 UI 하나만.
  - 중립 문구: `초기 비용만 비교한 결과예요. 매출·수익 지표는 계산하지 않아요.`
  - 미러 막대 비율은 **두 값 중 큰 값** 기준(합 기준이면 항목마다 축이 달라진다)
  - `levy` 는 **양쪽 다 null 일 때만** 행 제거. 한쪽만 null 이면 `해당 없음`.
  - 컴팩트 편집기는 `<select>` 4개 + 면적 `TextField`. `SimulationBrandSearch` 는 **그대로 재사용**
    (`variant='compact'` 추가하지 않는다).
  - 성공 시 `router.replace(buildSimulationCompareHref(...))` + `simulationReportQueryKey` 캐시 시딩.

### 주의사항

**저장소 상태 (2026-08-27 15:21 확인)**

- 로컬 develop `e00fb51` / **원격 `origin/develop` `a8f8600`** — 0번 참조.
- **열린 PR 없음** (`gh pr list --state open` 비어 있음).
- 워크트리 4개:
  `BossPickSeoul`(detached 1740cbb) · `bosspick-develop`(develop, clean) ·
  `bosspick-home`(미머지 `docs/fe/ai-report-page`, `frontend/.claude/` untracked) ·
  `bosspick-uxslop`(미머지 `feature/fe/ux-slop-sweep`, 원격에도 있음 fc0ae7e).
- `feature/fe/ux-slop-sweep` 은 **2026-08-14 스펙 커밋 1개**(`ux-slop-sweep-design.md`)뿐이고
  베이스가 2c6209c 라 develop 보다 한참 뒤처져 있다. 구현은 시작되지 않았다. 방치 중.
- **새 브랜치 베이스는 복구 전까지 로컬 `develop`**:
  ```bash
  git -C /Users/seonghoho/Documents/projects/nowdoboss/BossPickSeoul worktree add ../.worktrees/bosspick-simui -b feature/fe/simulation-ui-polish develop
  ```
  그 뒤 **`.env.local` 복사**(`.worktrees/bosspick-develop/frontend/.env.local`) + `pnpm install`.
  없으면 BFF 가 `AUTH_SESSION_SECRET must be set` 으로 500.

**환경**

- **dev 서버는 `preview_start` 로 못 띄운다** — 워크트리 경로에서 `getcwd: Operation not permitted`.
  **Bash 백그라운드 `pnpm dev -p 5173`** 을 쓰고 `preview_start` 는 `{url}` 로만 연다.
- **포트는 반드시 5173.** BE CORS 허용 목록이 `a8f8600` 에서 정리되며 `localhost:3000` 이 빠졌다.
  로컬 출처는 `http://localhost:5173` 하나, 카카오 JS 키도 5173 에만 등록. BFF 가 브라우저 `Origin` 을
  상류로 넘기므로 다른 포트는 POST 가 403.
- **브라우저 페인 탭이 죽는 일이 있다** — 하이드레이션 안 된 것처럼 보이고(클릭 무반응,
  `__react*` 없음) 요청도 안 나간다. dev 서버·`.next` 문제가 아니다. **`tabs_create` 로 새 탭.**
- 뷰포트가 `0x0` 이면 `resize_window` 로 크기를 준다(0x0 에서 `read_page` 가 빈 결과).
- `next dev` 는 `next-env.d.ts` 를 churn 시킨다. **커밋 전 `git checkout -- next-env.d.ts`.**

**분석 결과 화면 진입 경로** (토스트 회귀 확인 등에 필요)

`/analysis/result?...` 직접 진입은 안 그려진다. UI 로 밟는다:
`/analysis` → 자치구 → 행정동 → 상권 → 업종 → `분석 결과 보기`. 카카오 지도 로딩 ~10초.
검증된 조합: 강남구 · 역삼1동 · 역삼역발달상권 · 한식음식점.

**테스트 관용구**

- jsdom/testing-library 없이 **node 환경 + `renderToStaticMarkup` 문자열 단언**.
- **recharts 는 SSR 정적 렌더에서 차트 내부를 그리지 않는다** → 차트 값 단언은 공허하다.
  도넛 크기(2번)는 테스트로 고정하지 말고 **실화면 스크린샷**으로 확인.
- **zustand 세션은 `setState` 로 못 바꾼다**(서버 렌더가 `getServerSnapshot` 으로 초기 상태를 읽음).
  `vi.hoisted` + `vi.mock('@/stores/auth-store')` — `simulation-save-button.test.ts` 예시.
- JSX 삼항식 `cond ? ( ... ) : null` 안의 `{/* 주석 */}` 은 **파스 에러**. 삼항식 바깥에 둔다.
- 제어문자를 정규식 리터럴에 직접 쓰지 말 것(이스케이프). Bash 힙독은 툴이 거부하거나
  보이지 않는 문자를 박는다.

**단위·표기 함정 (시뮬레이션 전체)**

- **`formatLargeWon`(만원 입력) vs `status-formatters`(원 입력)** — 바꿔 쓰면 10,000배 오차.
- **`levy: 0` vs `levy: null`** — 0 은 "부담금 0원", null 은 "비프랜차이즈 해당 없음". falsy 검사 금지.
- **`lastId: 0` 은 "처음부터"가 아니라 "0번 다음부터"** — 첫 조회에 키를 싣지 마라.
- **`topAgeGroups[].salesAmount` 는 자치구×업종 전체 분기 매출**이지 사용자 점포 예상이 아니다.

**PR 규약** (`frontend/CLAUDE.md`)

```bash
gh pr create --base develop --title "[FE] <type>: <제목>" --body-file <파일> \
  --assignee seonghoho --label frontend-web
```

`frontend-web` 은 분류 꼬리표가 아니라 **Jenkins 배포 게이트**(fail-closed). 없으면 조용히 미배포.
머지는 squash(제목에 `(#N)` 이 붙고 머지 커밋 없음).

**닫힌 항목**

- 로그인 필요 검증 3건(시뮬레이션 저장 왕복 / 카카오 복귀 / 이어하기 토스트) — 사용자가 직접 완료.
- ~~Jenkins 미배포 의심 무효화~~ — **재개방.** 근거였던 "develop HEAD 가 배포됨"이 롤백으로 무효.

**후속으로 남은 별건** (변동 없음)

- FE 북마크 우회 제거 — dev Swagger 의 `MemberBookmarkItem.bookmarkId` 가 `string` 이 된 뒤
  `src/lib/api/user.ts:11-77` 우회 블록 + `user.test.ts` transform describe 4개 삭제.
- BE 요청 2건: 요청 본문 enum 불일치 시 `dataHeader` 봉투 없는 400 /
  `ShareTargetType` 에 시뮬레이션 상수 부재(시뮬레이션 공유 CTA 전제).
- `frontend/docs/api/openapi/` 스냅샷이 2026-08-07 기준 — 시뮬레이션·블루오션·분석보관함 미반영.
- 시뮬레이션 저장 버튼(`simulation-save-button.tsx`)은 아직 인라인 `Notice` 로 오류 표시.
  #146 토스트로 옮길지 검토 여지(급하지 않음).
