[//]: # '저장 경로: docs/features/auth/login.md'

# 인증(auth) — 로그인 세부 명세서

> **작성일**: 2026-07-21
> **공통 명세**: [인증 공통 명세](./auth.md)
> **대상**: 웹 (Next.js App Router)
> **작성자**: FE
> **상태**: 초안

이 문서는 [인증 공통 명세](./auth.md)의 **로그인 화면 및 플로우**를 상세화한다. 토큰 커스터디·세션 메커니즘은 [session-bff](./session-bff.md)에 위임하고 반복하지 않는다.

[[_TOC_]]

---

## D0. 배경 / 기획 의도

| 항목              | 내용                                                         |
| ----------------- | ------------------------------------------------------------ |
| 충족 요구사항     | 공통 명세 S3-2                                               |
| 해결하려는 문제   | 이메일/비밀번호 로그인 진입점 제공                           |
| 목표 동작 (to-be) | legacy 로그인 플로우 보존 + 새 디자인(DESIGN.md), 세션은 BFF |
| 구현 제외 범위    | 소셜 로그인 버튼 동작(D8 미결), 비밀번호 찾기(백엔드 미지원) |
| 연관 세부 기능    | [session-bff](./session-bff.md) D4-1                         |

---

## D1. 기능 개요

이메일/비밀번호를 입력받아 BFF 로그인을 호출하고, 성공 시 인증 상태로 전환·리다이렉트한다.

```
email/pw 입력 → 클라이언트 유효성 → POST /api/auth/login → 성공 시 auth-store 갱신 + redirect(원경로 또는 홈)
```

### D1-1. UI 진입점 / 기능 연결

> **Figma 디자인**: (링크 미정 — DESIGN.md 기준 신규 디자인 허용)
> **라우트**: `app/(auth)/login/page.tsx` · legacy: `containers/User/LoginContainer.tsx`

| UI 요소       | 사용자 동작 | 트리거 기능       | 결과 / UI 반영                     |
| ------------- | ----------- | ----------------- | ---------------------------------- |
| 이메일 입력   | 입력        | 클라이언트 유효성 | 형식 오류 표시                     |
| 비밀번호 입력 | 입력        | 클라이언트 유효성 | 필수/형식 표시                     |
| 로그인 버튼   | 클릭        | D4-1              | 로딩 → 성공 리다이렉트 / 실패 에러 |
| 회원가입 링크 | 클릭        | —                 | `/register` 이동                   |

---

## D2. 동작 요구사항

| #   | 요구사항                                        | 상세 참조 |
| --- | ----------------------------------------------- | --------- |
| 1   | 이메일 형식·비밀번호 필수 클라이언트 유효성     | D4-1      |
| 2   | 로그인 성공 시 `redirect` 쿼리 또는 홈으로 이동 | D4-1      |
| 3   | 실패 시 백엔드 메시지 기반 에러 표시, 입력 보존 | D5        |
| 4   | 로그인 중 버튼 비활성·로딩 표시                 | D4-1      |

---

## D3. 아키텍처 / 시스템 설계

로그인 화면은 client component. 제출은 [session-bff](./session-bff.md) D4-1의 `/api/auth/login`을 호출한다. 토큰·세션 처리는 서버측(session-bff)이 전담하므로 이 화면은 토큰을 다루지 않는다.

| 모듈                        | 책임                                           |
| --------------------------- | ---------------------------------------------- |
| `app/(auth)/login/page.tsx` | 로그인 화면(폼, 유효성, 제출)                  |
| `src/components/auth/*`     | 입력 컴포넌트(legacy `components/User/*` 대응) |

---

## D4. 상세 동작 정의

### D4-1. 로그인 제출

| 사용 엔드포인트              | 용도        | 응답 → 내부 모델          | 비고                                                      |
| ---------------------------- | ----------- | ------------------------- | --------------------------------------------------------- |
| `POST /api/auth/login` (BFF) | 로그인 대행 | `{memberId}` → auth-store | 실제 백엔드 `/auth/login` 및 세션 봉인은 session-bff D4-1 |

동작: 폼 제출 → 클라이언트 유효성(이메일 형식, 비밀번호 비어있지 않음) → `POST /api/auth/login` → 성공 시 auth-store `isAuthenticated=true`, `memberId` 설정 → `redirect` 쿼리 경로(없으면 홈)로 이동. 실패 시 에러 표시.

---

## D5. 비즈니스 로직

| 조건                   | 결과                                                      |
| ---------------------- | --------------------------------------------------------- |
| 유효성 실패            | 제출 차단, 필드 에러 표시                                 |
| 로그인 성공            | auth-store 갱신 + 리다이렉트                              |
| 로그인 실패(자격 오류) | 에러 메시지 표시, 비밀번호 필드 초기화(선택), 이메일 보존 |
| 네트워크/5xx           | 일반 에러 표시(금지 문구 회피, DESIGN.md 카피 규칙)       |

---

## D6. 주의사항

| 항목          | 내용                                                                                                                                                                                                                                           |
| ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 토큰 미취급   | 이 화면은 토큰을 받지도 저장하지도 않는다(서버 세션 전담)                                                                                                                                                                                      |
| redirect 검증 | `redirect` 쿼리는 오픈 리다이렉트 방지를 위해 내부 경로만 허용. `safeRedirect`는 `/`로 시작 + `//`로 시작하지 않음 + 백슬래시(`\`) 미포함을 모두 만족해야 통과(Next URL 정규화로 `/\evil.com`→외부도메인 되는 우회 차단). 불충족 시 `/`로 대체 |
| 이미 로그인   | 인증 상태로 `/login` 진입 시 홈으로 리다이렉트                                                                                                                                                                                                 |

---

## D7. 테스트케이스

| TC ID        | 목적            | 실행                                  | 기대 결과                |
| ------------ | --------------- | ------------------------------------- | ------------------------ |
| TC-LOGIN-001 | 유효성          | 잘못된 이메일 형식 제출               | 제출 차단 + 필드 에러    |
| TC-LOGIN-002 | 성공 리다이렉트 | `?redirect=/analysis`로 진입해 로그인 | 성공 후 `/analysis` 이동 |
| TC-LOGIN-003 | 실패 표시       | 잘못된 자격                           | 에러 메시지, 이메일 보존 |
| TC-LOGIN-004 | 이미 로그인     | 인증 상태로 `/login` 접근             | 홈 리다이렉트            |

---

## D8. 미결 사항

| #   | 항목                                               | 담당    | 기한 |
| --- | -------------------------------------------------- | ------- | ---- |
| 1   | 소셜 로그인 버튼 — 백엔드 OAuth 부재로 비활성/숨김 | BE 선행 | 미정 |
| 2   | 비밀번호 찾기 — 백엔드 미지원                      | BE 선행 | 미정 |

---

## 변경 이력

| 버전 | 날짜       | 변경 내용 | 작성자 |
| ---- | ---------- | --------- | ------ |
| 1.0  | 2026-07-21 | 최초 작성 | FE     |
