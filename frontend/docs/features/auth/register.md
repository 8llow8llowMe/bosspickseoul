[//]: # '저장 경로: docs/features/auth/register.md'

# 인증(auth) — 회원가입 세부 명세서

> **작성일**: 2026-07-21
> **공통 명세**: [인증 공통 명세](./auth.md)
> **대상**: 웹 (Next.js App Router)
> **작성자**: FE
> **상태**: 개정(v2.0 — 이메일 인증 2단계 반영)

이 문서는 [인증 공통 명세](./auth.md)의 **회원가입 화면 및 플로우**를 상세화한다. 백엔드가 **이메일 인증(코드 발송·검증)을 회원가입 선행 조건**으로 요구하므로(`MEMBER_006`), send-code → verify-code → signup 순서를 FE에서 오케스트레이션한다.

[[_TOC_]]

---

## D0. 배경 / 기획 의도

| 항목              | 내용                                                                                                            |
| ----------------- | --------------------------------------------------------------------------------------------------------------- |
| 충족 요구사항     | 공통 명세 S3-3                                                                                                  |
| 해결하려는 문제   | 신규 회원 가입 진입점 + 이메일 소유 검증                                                                        |
| 기존 동작 (as-is) | FE가 단일 `signup`만 호출 → 백엔드가 이메일 인증을 요구하며 `MEMBER_006`로 실패(회원가입 불가)                  |
| 목표 동작 (to-be) | 인증코드 발송 → 코드 검증 → 가입의 3단계를 단일 페이지에서 progressive disclosure로 처리. 인증 완료 후에만 가입 |
| 구현 제외 범위    | 실시간 이메일 중복확인(전용 엔드포인트 없음 — 가입 응답으로 판별). 2단계 가입은 단일 페이지 순차 방식으로 통합  |
| 연관 세부 기능    | [login](./login.md), [social-login](./social-login.md)                                                          |

---

## D1. 기능 개요

이메일 인증을 완료한 뒤 비밀번호·이름·닉네임을 입력받아 가입을 요청하고, 성공 시 로그인으로 유도한다.

```
이메일 입력 → send-code(발송) → 코드 입력 → verify-code(검증)
  → 검증 성공 → 나머지 필드 활성화 → signup(가입) → 성공 시 /login 유도
```

> **인증 상태 보관**: `verify-code` 응답은 `Response<Void>`로 별도 인증 토큰을 주지 않는다. 백엔드가 서버측에서 "인증 완료된 이메일"을 기억하므로(추정: Redis TTL), FE는 **동일 이메일**로 send → verify → signup을 순서대로 호출하면 된다. signup 요청 바디에 인증 토큰/플래그는 포함하지 않는다. (dev API 실측으로 검증 — D8-1)

### D1-1. UI 진입점 / 기능 연결

> **디자인**: DESIGN.md 기준. `AuthShell` + 기존 프리미티브 재사용, 새 토큰 추가 없음.
> **라우트**: `app/(auth)/register/page.tsx` → `src/components/auth/register-form.tsx`
> legacy 2단계(`register/general`)는 **단일 페이지 순차(progressive disclosure)** 로 통합한다.

| UI 요소                | 사용자 동작 | 트리거 기능    | 결과 / UI 반영                                               |
| ---------------------- | ----------- | -------------- | ------------------------------------------------------------ |
| 이메일 입력 + 발송버튼 | 입력 → 클릭 | D4-1 send-code | 발송 성공 → 코드 입력칸 등장 + 재전송 쿨다운 타이머          |
| 코드 입력 + 확인버튼   | 입력 → 클릭 | D4-2 verify    | 검증 성공 → 이메일/코드 잠금(성공 배지) + 나머지 필드 활성화 |
| 비밀번호/이름/닉네임   | 입력        | 클라 유효성    | 필드별 오류 표시                                             |
| 가입 버튼              | 클릭        | D4-3 signup    | 로딩 → 성공 시 로그인 유도 / 실패 에러                       |

---

## D2. 동작 요구사항

| #   | 요구사항                                                        | 상세 참조 |
| --- | --------------------------------------------------------------- | --------- |
| 1   | 이메일 인증(발송·검증) 완료 전에는 가입 제출 불가(버튼 비활성)  | D4, D5    |
| 2   | 이메일 변경 시 인증 상태 리셋(코드칸·잠금 해제, 재발송 요구)    | D5        |
| 3   | 재전송 쿨다운(예: 180초) 동안 발송 버튼 비활성 + 남은 시간 표시 | D4-1      |
| 4   | 백엔드 제약과 동일한 클라이언트 유효성                          | D4-3      |
| 5   | 가입 성공 시 로그인 화면 유도                                   | D5        |
| 6   | 실패(코드 만료/불일치·중복 이메일 등) 시 백엔드 메시지 표시     | D5, D6    |

---

## D3. 아키텍처 / 시스템 설계

가입 화면은 client component. 세 호출 모두 인증 불필요 공개 엔드포인트로, 기존 **범용 BFF 프록시**(`/api/bff/...`)를 그대로 경유한다(세션 없이 Bearer 없이 포워드). **새 BFF 라우트 불필요.**

| 모듈                                    | 책임                                         |
| --------------------------------------- | -------------------------------------------- |
| `app/(auth)/register/page.tsx`          | 라우트 진입(메타데이터)                      |
| `src/components/auth/register-form.tsx` | 가입 화면(단계 상태·유효성·제출)             |
| `src/lib/api/*` (선택)                  | send-code/verify-code/signup 호출 유틸(선택) |

**단계 상태(step state)**: `email-entry` → `code-sent` → `verified` → (제출) 의 로컬 상태 머신으로 관리.

---

## D4. 상세 동작 정의

### D4-1. 인증코드 발송

| 엔드포인트                                                                 | 요청      | 응답             |
| -------------------------------------------------------------------------- | --------- | ---------------- |
| `POST /api/bff/auth/email/send-code` → `POST /api/v1/auth/email/send-code` | `{email}` | `Response<Void>` |

- 유효 이메일 형식 확인 후 발송. 성공 시 코드 입력 UI 노출 + 재전송 쿨다운 시작.

### D4-2. 인증코드 검증

| 엔드포인트                                                                     | 요청            | 응답             |
| ------------------------------------------------------------------------------ | --------------- | ---------------- |
| `POST /api/bff/auth/email/verify-code` → `POST /api/v1/auth/email/verify-code` | `{email, code}` | `Response<Void>` |

- 성공 시 이메일/코드 잠금 + 나머지 필드 활성화. 실패 시 코드 오류 메시지.

### D4-3. 가입 제출

| 엔드포인트                                                     | 요청                                | 응답             |
| -------------------------------------------------------------- | ----------------------------------- | ---------------- |
| `POST /api/bff/members/signup` → `POST /api/v1/members/signup` | `{email, password, name, nickname}` | `Response<Void>` |

**클라이언트 유효성 (백엔드 제약과 동일):**
| 필드 | 제약 |
|---|---|
| email | 이메일 형식 필수(인증 완료된 이메일과 동일) |
| password | 공백 없이 영문자+숫자+특수문자 포함 8~20자 (`^(?=.*[A-Za-z])(?=.*\d)(?=.*[특수문자])\S{8,20}$`) |
| name | 필수, 최대 10자 |
| nickname | 필수, 최대 10자 |

---

## D5. 비즈니스 로직

| 조건                      | 결과                                                       |
| ------------------------- | ---------------------------------------------------------- |
| 이메일 형식 오류          | 발송 차단, 필드 에러                                       |
| 코드 발송 성공            | 코드 입력 UI 노출, 쿨다운 시작                             |
| 코드 검증 성공            | `verified` 상태 전환, 이메일/코드 잠금, 나머지 필드 활성화 |
| 이메일 변경(검증 후)      | 인증 상태 리셋 → 재발송·재검증 필요                        |
| 미인증 상태 가입 시도     | 버튼 비활성(방어). 강제 호출 시 백엔드 `MEMBER_006` 표시   |
| 유효성 실패               | 제출 차단, 필드 에러                                       |
| 가입 성공                 | 성공 안내 → `/login`으로 유도(백엔드 signup 토큰 미발급)   |
| 가입 실패(중복 이메일 등) | 백엔드 메시지 표시                                         |

---

## D6. 주의사항

| 항목            | 내용                                                                                         |
| --------------- | -------------------------------------------------------------------------------------------- |
| 비밀번호 정규식 | 백엔드 정규식과 **정확히 동일**하게 유지                                                     |
| 인증 상태 결합  | send/verify/signup의 이메일이 **동일 문자열**이어야 함(trim·소문자 정책 백엔드와 일치 확인)  |
| 가입 후 상태    | 백엔드 signup은 토큰 미발급 → 가입 직후 로그인 별도 필요                                     |
| 중복확인        | 실시간 이메일 중복확인 엔드포인트 없음 → 제출 시 서버 응답으로만 판별                        |
| 에러 코드 매핑  | `MEMBER_006`(미인증), 코드 만료/불일치 등 resultCode → 사용자 문구 매핑(공통명세 S2 #7 보존) |

---

## D7. 테스트케이스

| TC ID      | 목적           | 실행                        | 기대 결과                         |
| ---------- | -------------- | --------------------------- | --------------------------------- |
| TC-REG-001 | 코드 발송      | 유효 이메일로 발송          | 코드 입력 UI 노출, 쿨다운 시작    |
| TC-REG-002 | 코드 검증 성공 | 올바른 코드 입력            | verified 전환, 나머지 필드 활성화 |
| TC-REG-003 | 코드 검증 실패 | 틀린/만료 코드 입력         | 코드 에러 메시지, 미검증 유지     |
| TC-REG-004 | 이메일 변경    | 검증 후 이메일 수정         | 인증 상태 리셋, 재발송 요구       |
| TC-REG-005 | 비밀번호 규칙  | 규칙 미충족 비밀번호        | 필드 에러, 제출 차단              |
| TC-REG-006 | 가입 성공      | 인증 완료 후 유효 입력 제출 | 성공 안내 → 로그인 유도           |
| TC-REG-007 | 중복 이메일    | 기존 이메일로 가입          | 백엔드 실패 메시지 표시           |

---

## D8. 미결 사항

| #   | 항목                                                                                                                                                                                                                                    | 담당     | 기한    |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------- |
| 1   | 인증 상태 전달 방식 — **확정(dev 실측)**: 백엔드가 서버측에서 인증 완료 이메일을 기억(동일 이메일로 send→verify→signup 순서 호출, signup 바디에 토큰/플래그 불필요). 미인증 상태 signup 시 `MEMBER_006`, 코드 오류 시 `AUTH_004`로 검증 | FE(실측) | 완료    |
| 2   | 이메일 정규화 정책(trim·소문자) 백엔드 일치 여부                                                                                                                                                                                        | FE/BE    | 구현 중 |
| 3   | 실시간 이메일 중복확인 API — 백엔드 미지원                                                                                                                                                                                              | BE 선행  | 미정    |
| 4   | 가입 직후 자동 로그인 여부(백엔드 signup 토큰 미발급)                                                                                                                                                                                   | 기획/BE  | 미정    |

---

## 변경 이력

| 버전 | 날짜       | 변경 내용                                            | 작성자      |
| ---- | ---------- | ---------------------------------------------------- | ----------- |
| 1.0  | 2026-07-21 | 최초 작성                                            | FE          |
| 2.0  | 2026-08-07 | 이메일 인증 2단계(send-code/verify-code) 플로우 반영 | Claude Code |
