# 상권 추천 — UX 후속 3건 세부 명세서

> **작성일**: 2026-08-28
> **공통 명세**: 없음 — `recommend` 공통 명세 미작성([\_index](../_index.md) 참고). 현행 정본은 코드다.
> **연관 명세**: [조건 선택 UX](./condition-selector.md), [모바일/태블릿 반응형 바텀시트](../analysis/mobile-responsive.md)
> **대상**: 웹 (Next.js App Router) — `/recommend`
> **작성자**: Claude
> **상태**: 초안 (구현 대기)

2026-08-28 리뷰에서 나온 7건 중 **즉시 고친 4건을 뺀 나머지 3건**을 구현 수준으로 적는다.
셋 다 판단이 필요하거나 다른 작업이 선행돼야 해서 한 번에 처리하지 않았다.

| #     | 항목                                                           | 왜 즉시 못 고쳤나                                           |
| ----- | -------------------------------------------------------------- | ----------------------------------------------------------- |
| **A** | 업종 선택 UX — 버튼 모양·찾기 어려움                           | 인기 업종·아이콘 도입이 **데이터와 디자인 결정**을 요구한다 |
| **B** | 결과 지도 — 결과 없을 때 카메라 고정, 결과 클릭 시 상권 폴리곤 | 상권 **경계 조인**(백엔드 누락 대응)이 선행돼야 한다        |
| **C** | 모바일 바텀시트를 분석 방식으로 통일                           | 두 구현이 각각 500~650줄이고 **공유 모듈 승격**이 필요하다  |

### 같은 리뷰에서 이미 고친 것 (`8705ad5f`)

- 분석 단계 탭·추천 결과 카드(상권명·추천 이유)·선택 목록 항목의 **말줄임 제거**
- 검색 입력 **포커스 이중 테두리** → 한 줄(1px → 2px 두께 변화)
- 결과 단계 행정동을 **채움 없는 중립 외곽선**으로 (선택된 것처럼 보이던 문제)
- 지도 좌상단 **「추천 범위 고정」 뱃지 제거**

---

## A. 업종 선택 UX

### A-0. 배경

| 항목              | 내용                                                                                                                          |
| ----------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| 해결하려는 문제   | 업종 31개가 6그룹 세로 목록으로만 있어 원하는 업종을 **찾기 어렵다**. 항목 버튼의 모양도 선택지가 아니라 이동 링크처럼 읽힌다 |
| 기존 동작 (as-is) | `OptionPicker` `layout='list'` — 한 줄에 하나씩, 우측에 `ChevronRight`. 검색은 있다                                           |
| 목표 동작 (to-be) | 자주 고르는 업종에 **먼저 닿고**, 항목이 「선택지」로 읽히게 한다                                                             |
| 구현 제외 범위    | 지역(자치구·행정동) 선택 — 칩 격자로 충분하다                                                                                 |

### A-1. 지금 무엇이 잘못됐나

**1. `ChevronRight` 가 거짓말을 한다.** 목록 항목의 우측 꺾쇠는 「누르면 다음 화면으로 간다」는
관용 표현인데, 실제로는 그 자리에서 **선택되고 조건 바로 되돌아온다.** 선택지에는 라디오·체크
같은 선택 어포던스가 맞다. 선택된 항목만 `Check` 로 바뀌는 현재 방식은 **선택 전에는 아무 단서가
없다**는 뜻이기도 하다.

**2. 31개를 균등하게 늘어놓는다.** 「커피-음료」·「한식음식점」처럼 압도적으로 많이 고르는
업종과 「자전거 및 기타운송장비」가 같은 무게로 같은 거리에 있다. 스크롤 위치는 카탈로그 순서가
정하지 사용 빈도가 정하지 않는다.

**3. 세로 한 줄 배치가 화면을 길게 쓴다.** 지역 단계는 3열 칩 격자인데 업종만 1열이라, 같은
선택 뷰인데 밀도가 갑자기 달라진다.

### A-2. 방향 — 「인기 먼저, 그다음 전체」

```
┌─────────────────────────────┐
│  [ 업종 검색            ]   │
├─────────────────────────────┤
│  자주 찾는 업종              │
│  ☕커피-음료  🍚한식  🍗치킨 │   ← 6~8개, 큰 타깃
│  🍺호프-간이  💇미용  🏪편의 │
├─────────────────────────────┤
│  음식점                      │
│  중식음식점   일식음식점 …   │   ← 전체, 2열 격자
│  학원                        │
│  …                           │
└─────────────────────────────┘
```

- **인기 섹션**을 맨 위에 둔다. 검색을 치기 전에 이미 대부분이 여기서 끝난다.
- 전체 목록은 **2열 격자**로 바꿔 지역 단계와 밀도를 맞춘다. 이름이 긴 업종이 있으므로
  칩(1열 중앙정렬)이 아니라 좌측정렬 2열이다.
- 검색은 그대로 두되, 검색 중에는 인기 섹션을 숨긴다(중복 노출 방지).

### A-3. 착수 전 정해야 할 것

| #     | 결정                            | 선택지                                                                                                                                                                                                                        |
| ----- | ------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A-3-1 | **인기 업종을 무엇으로 정하나** | (a) FE 상수로 6~8개 하드코딩 (b) 백엔드에 「업종별 조회수/추천수」 요청 (c) 사용자 최근 선택 `localStorage`                                                                                                                   |
| A-3-2 | **아이콘을 넣을 것인가**        | 31개 전부에 의미가 통하는 아이콘을 붙이기는 어렵다. `lucide-react` 로 「커피·한식·치킨·미용·편의점」 같은 대표 업종은 되지만 「기타법무서비스」는 안 된다. **인기 섹션에만** 붙이고 전체 목록은 텍스트로 두는 절충이 유력하다 |
| A-3-3 | **선택 어포던스**               | (a) 좌측 라디오 (b) 선택 시에만 우측 `Check`(현행) + `ChevronRight` 제거 (c) 칩처럼 배경색으로만                                                                                                                              |

**권장**: A-3-1 은 **(a) 하드코딩으로 시작**한다. 백엔드 없이 오늘 되고, 나중에 (b)로 갈아끼울
때 상수만 바꾸면 된다. 다만 **무엇을 인기로 볼지는 사람이 정해야 한다** — 임의로 고르면 근거가
없다. A-3-2 는 **인기 섹션만**. A-3-3 은 **(b)** — `ChevronRight` 만 걷어내면 거짓말이 사라지고
변경 폭이 가장 작다.

### A-4. 주의사항

- `OptionPicker` 는 분석 패널도 쓴다. **2열 격자·인기 섹션을 기본값으로 만들면 분석이 함께
  바뀐다.** 새 `layout` 값(예: `'grid-wide'`)과 옵션 prop 으로 열고, 분석은 현행 그대로 둔다.
- 분석의 업종 단계는 **백엔드가 주는 `serviceType.name`** 으로 그룹핑하고 추천은 **정적
  카탈로그**를 쓴다. 인기 섹션을 공유 부품에 넣는다면 두 출처 모두에서 코드 매칭이 되는지
  확인해야 한다.

---

## B. 결과 지도 — 카메라와 상권 폴리곤

### B-0. 배경

| 항목              | 내용                                                                                                                                       |
| ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| 해결하려는 문제   | 「상권 추천받기」를 누르면 **행정동으로 카메라가 이동**한다. 추천 결과가 없어도 이동하고, 결과가 있어도 개별 상권은 지도에 나타나지 않는다 |
| 기존 동작 (as-is) | `fitPoints` 가 선택 행정동 경계로 잡혀 카메라가 그리로 맞춰진다. 결과 상권 폴리곤은 **한 개도 그려지지 않는다**                            |
| 목표 동작 (to-be) | 결과가 없으면 카메라를 그대로 둔다. 결과 목록에서 항목을 고르면 **그 상권**이 폴리곤으로 표시된다                                          |
| 선행 조건         | **B-1 경계 조인** — 이게 안 되면 나머지가 무의미하다                                                                                       |

### B-1. 선행 — 상권 경계 조인 (condition-selector D8 과 동일 건)

Kakao SDK `Polygon` 생성자를 계측해 확인했다. 결과 단계에서 생기는 폴리곤은 컨텍스트 영역
**1개뿐**, 결과 폴리곤은 **0개**다.

백엔드 `GET /map/commercials/{code}/profile` 이 `centerLng: null, centerLat: null,
boundaryCoords: []` 를 내려준다(강남구 역삼2동 / CS100010 / 20233 재현). `buildRecommendationMapItems`
(`recommend-map-model.ts:100`)는 center 만 후보 목록에서 폴백하고 경계는 폴백하지 않아 빈
배열이 되고, `drawPolygon` 이 `points.length < 3` 으로 null 을 반환한다.

후보 목록 타입 `CommercialArea`(`types/recommend.ts:27`)에도 경계가 없다 — 경계는 뷰포트 질의
`GET /map/commercials?lngSW=…`(= `commercialAreas` prop)에만 있다.

**착수 전 정해야 할 것**: 뷰포트 밖 상권은 `commercialAreas` 에 없으므로 지도를 패닝하면
폴리곤이 생겼다 사라진다.

| 선택지                              | 내용                                                                | 비고                  |
| ----------------------------------- | ------------------------------------------------------------------- | --------------------- |
| (a) 뷰포트 목록에서 조인            | `buildRecommendationMapItems` 에 `commercialAreas` 를 추가로 넘긴다 | 싸다. 패닝 시 깜빡임  |
| (b) 결과 코드 경계를 따로 질의·캐시 | Top N 코드로 한 번 더 조회해 들고 있는다                            | 안정적. 요청 1회 추가 |
| (c) 백엔드 수정 요청                | profile 응답에 경계를 채워 달라고 한다                              | 근본. BE 선행         |

**권장**: **(b)**. 결과는 최대 5건이라 한 번 받아 두면 끝이고, 패닝 중 폴리곤이 사라지는 것은
사용자가 「버그」로 읽는 종류의 흔들림이다. (c)는 병행 요청하되 기다리지 않는다.

### B-2. 결과가 없으면 카메라를 옮기지 않는다

「추천받기」 결과가 0건인데 행정동으로 확대되면, 사용자는 **아무것도 없는 화면으로 끌려간다.**
제출 시점이 아니라 **결과를 받은 뒤** 카메라를 정한다.

| 상황         | 카메라                         |
| ------------ | ------------------------------ |
| 결과 ≥ 1건   | 결과 상권들을 모두 담는 bounds |
| 결과 0건     | **직전 카메라 유지**           |
| 결과 로딩 중 | 유지                           |

### B-3. 결과 항목을 고르면 그 상권을 표시한다

목록·마커에서 상권을 고르면 그 상권 폴리곤이 `selected` 상태(2.5px, 점수 농도 +0.20)가 되고
카메라가 그 상권으로 맞춰진다. 스타일 계약은 이미 있다(`getResultPolygonStyle`) — B-1 로 경계만
생기면 동작한다.

행정동 컨텍스트는 `8705ad5f` 에서 채움 없는 중립 외곽선으로 낮췄다. 그대로 둔다.

---

## C. 모바일 바텀시트 통일

### C-0. 배경

| 항목              | 내용                                                                                                     |
| ----------------- | -------------------------------------------------------------------------------------------------------- |
| 해결하려는 문제   | `/recommend` 바텀시트가 `/analysis` 와 다르게 동작해 같은 서비스 안에서 조작감이 어긋난다                |
| 기존 동작 (as-is) | 두 화면이 **각자 구현**을 갖는다. `recommend-mobile-sheet.tsx` 652줄 / `analysis-mobile-sheet.tsx` 507줄 |
| 목표 동작 (to-be) | 분석 시트의 규격·상태 헬퍼를 **공유 모듈로 승격**하고 추천이 그것을 쓴다                                 |

### C-1. 실측 차이

|                | `/analysis`                                                              | `/recommend`                                     |
| -------------- | ------------------------------------------------------------------------ | ------------------------------------------------ |
| collapsed 높이 | **72px** — 내용 첫 줄이 보인다                                           | **44px** — 손잡이만 보인다                       |
| expanded       | 뷰포트 × 0.72                                                            | 뷰포트 × 비율, **최소 지도 높이** 보장 로직 별도 |
| 상태 헬퍼      | `lib/analysis/analysis-sheet-state.ts` — **공유 모듈, 단위 테스트 있음** | 컴포넌트 파일 안에 내장                          |
| 뷰 전환        | 시트가 `SheetView` 를 직접 들고 크로스페이드                             | 패널이 뷰를 정하고 시트는 높이만                 |

**44px 이 체감 차이의 핵심으로 보인다.** 접었을 때 아무 내용도 안 보이면 시트가 「닫힌 것」처럼
읽혀서, 다시 펼칠 단서가 손잡이뿐이다.

### C-2. 방향

1. `analysis-sheet-state.ts` 를 `src/lib/map/bottom-sheet-state.ts` 로 승격한다(이름에서 analysis 제거).
   `ANALYSIS_SHEET_*` 상수는 중립 이름으로 바꾸고 분석은 재수출로 무회귀를 유지한다.
2. 추천 시트가 그 모듈을 쓰게 하고, 내장 헬퍼(`resolveRecommendationSheetSnap` 등)를 지운다.
3. collapsed 를 **72px 로 맞춘다.**
4. 추천의 **최소 지도 높이 보장**은 살릴지 판단한다 — 분석에는 없는 로직이고, 지도가 주인공인
   화면이라 근거가 있다. 살린다면 공유 모듈의 **옵션**으로 올린다.

### C-3. 주의사항

- 추천 시트는 `criteria`·`picker`·`results` **3뷰**를 탄다. 조건 선택 UX 에서 `picker` 를
  더했으므로(`isRecommendationSheetInteractive`), 통일 작업에서 이 판정이 빠지지 않게 한다.
- 선택 뷰는 목록이 길어 **펼침 스냅**으로 열려야 한다(`pickerOpened` 가 이미 그렇게 한다).
- 시트 관련 단위 테스트가 양쪽에 있다. 헬퍼를 옮기면 테스트도 함께 옮긴다.

---

## D7. 테스트케이스

node 환경 + `renderToStaticMarkup` 문자열 assertion 컨벤션을 따른다.

| #    | 대상               | 케이스                           | 기대                                     |
| ---- | ------------------ | -------------------------------- | ---------------------------------------- |
| T-A1 | 업종 목록          | 항목 렌더                        | 선택 안 된 항목에 `ChevronRight` 가 없다 |
| T-A2 | 인기 업종 상수     | 검색어 있음                      | 인기 섹션이 숨는다                       |
| T-A3 | `OptionPicker`     | 분석 패널 호출                   | 기존 레이아웃 무회귀(인기 섹션 없음)     |
| T-B1 | 경계 조인          | profile 경계 비고 후보 경계 있음 | `boundaryCoords` 가 후보에서 채워진다    |
| T-B2 | 결과 카메라        | 결과 0건                         | `fitPoints` 가 `null`(직전 카메라 유지)  |
| T-B3 | 결과 카메라        | 결과 3건                         | 3건을 모두 담는 bounds                   |
| T-B4 | 결과 선택          | 상권 선택                        | 그 상권이 `selected` 스타일              |
| T-C1 | 공유 시트 헬퍼     | collapsed 높이                   | 두 화면 모두 72px                        |
| T-C2 | 시트 상호작용 판정 | `'picker'`                       | `true` (조건 선택 뷰가 빠지지 않았다)    |

**사람 검증**: 업종 인기 섹션 체감, 결과 폴리곤 표시·선택, 시트 드래그 스냅과 관성.

---

## D8. 미결 사항

- **인기 업종 목록의 근거** (A-3-1) — 하드코딩으로 시작하되 **무엇을 인기로 볼지는 사람이 정한다**
- **경계 조인 방식** (B-1) — (b) 별도 질의 권장, 백엔드 수정(c)은 병행 요청
- **추천 시트의 최소 지도 높이 보장을 살릴지** (C-2-4)
- `/recommend` URL 상태 — [condition-selector D8-1](./condition-selector.md) 과 동일 건. B 와 함께
  하면 결과 화면이 새로고침·공유에서 복원된다
