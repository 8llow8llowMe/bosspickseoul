# 상권 추천 — 공통 개발 명세서

> **작성일**: 2026-09-10
> **대상**: 웹 (Next.js App Router) — `/recommend`, `/recommend/compare`
> **작성자**: Claude
> **상태**: 명세 완료 (사후 작성 — 구현·머지된 화면을 실측해 정본으로 옮겼다)

[[_TOC_]]

---

## S0. 배경 / 기획 의도

| 항목               | 내용                                                                                                                                                                                                                                                                                                                       |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 요청자 / 요청팀    | BossPickSeoul 프론트엔드                                                                                                                                                                                                                                                                                                   |
| 요청일             | 2026-07 (마이그레이션 착수) · 세부명세 3종은 2026-08-28 ~ 08-31                                                                                                                                                                                                                                                            |
| 원본 기획 문서     | [상권 추천 PR #54](https://github.com/8llow8llowMe/bosspickseoul/pull/54), `DESIGN.md` S-REC, 저장된 OpenAPI(`commercial-analysis.json` · `region-map.json`), 세부명세 3종(아래 S4)                                                                                                                                        |
| 요청 배경          | 창업 예정자가 「이 동네에서 이 업종이면 어느 상권이 나은가」를 물을 곳이 없었다. 분석(`/analysis`)은 상권 하나를 깊게 보는 도구라 **후보를 고르는 단계**가 비어 있었다.                                                                                                                                                    |
| 기존 동작 (as-is)  | 레거시 NowDoBoss 는 네이티브 `<select>` 세 개(자치구·행정동·업종)가 세로로 쌓여 지도를 가리고, 결과는 카드 목록만 있었다. 선택 상태가 URL 에 없어 새로고침·공유에서 화면이 사라졌고, 비교는 화면이 추천 응답을 조립해 N열 표를 만들었다.                                                                                   |
| 목표 동작 (to-be)  | 조건 바 한 줄 + 패널 내 선택 뷰 + 지도 뱃지 지름길로 조건을 고르고, 후보 상권 중 **Top 5** 를 점수·추천 이유와 함께 보여 준다. 조건·결과 단계·고른 상권은 URL 이 나른다. 비교는 백엔드 `GET /commercials/compare` 를 정본으로 **정확히 두 상권**을 나란히 두고, 그 결과로 커뮤니티 글쓰기(비교 초안)를 연다.               |
| 구현 제외 범위     | 백엔드 변경(경계 조인·서비스 코드 전체 목록·N개 비교 계약), 분석 시점(`periodCode`) 선택 UI, 추천 카메라의 URL 보존(`c`), 분석 ↔ 추천 딥링크(D8-2), 목록 호버 → 지도 하이라이트(D8-3), 초성 검색(D8-4)                                                                                                                     |
| 연관 기능 / 의존성 | `/analysis`(지도 셸·`OptionPicker` 공유·`lib/map/bottom-sheet-state`), `/community/register`(비교 초안 `POST /community/posts/drafts/commercial-comparisons`), `/profile/bookmarks/*`(상권 북마크·화면 보관함 `COMMERCIAL_COMPARISON`), 홈 03단계 미리보기(`use-recommend-preview` 가 같은 API 연쇄를 쓴다), 공통 BFF·인증 |

### API 정본

경로·필드·인증은 저장된 OpenAPI 를 따른다. 코드나 레거시 구현과 충돌하면 스냅샷이 이긴다.

- `frontend/docs/api/openapi/commercial-analysis.json` — 추천 · 비교 · 상권 프로필
- `frontend/docs/api/openapi/region-map.json` — 지역 목록 · 지도 영역 좌표
- `frontend/docs/api/openapi/auth-member.json` — 상권 북마크
- `frontend/docs/api/openapi/endpoints.md`

---

## S1. 기능 개요

사용자는 자치구 → 행정동 → 업종을 고르고, 그 행정동의 상권 중 업종 기준 **상위 5곳**을 점수와 추천 이유로 받아 본다. 지도는 선택을 돕고 결과를 그려 주는 가속기이고, **목록·패널·URL 이 정본**이다. 결과에서 두 상권을 담아 비교하고, 비교 결과를 근거로 커뮤니티 글을 시작할 수 있다.

```text
조건 선택(자치구·행정동·업종) → 행정동 상권 목록 → 업종별 추천 Top 5 → 상권 프로필 → (두 곳 담기) 비교 → 비교 초안으로 글쓰기
```

| 단계        | 호출                                                                        | 인증     |
| ----------- | --------------------------------------------------------------------------- | -------- |
| 행정동 목록 | `GET /regions/districts/{districtCode}/administrations`                     | 불필요   |
| 상권 목록   | `GET /regions/districts/{d}/administrations/{a}/commercials`                | 불필요   |
| 추천        | `GET /commercials/recommendations/by-service` (`commercialCodes[]`·`topN`)  | 불필요   |
| 상권 프로필 | `GET /map/commercials/{commercialCode}/profile`                             | 불필요   |
| 지도 뱃지   | `GET /map/districts` · `/map/administrations` · `/map/commercials` (bounds) | 불필요   |
| 비교        | `GET /commercials/compare` (`leftCommercialCode`·`rightCommercialCode`)     | 불필요   |
| 북마크      | `GET/POST/DELETE /members/me/bookmarks`                                     | **필요** |

---

## S2. 공통 요구사항

| #   | 요구사항                                                                                                                                                                                           | 상세 참조                                                                                                                   |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| 1   | `/recommend`·`/recommend/compare` 는 **비로그인으로 끝까지** 쓸 수 있다. 로그인은 상권 **저장(북마크)** 과 비교 초안 **글 저장** 시점에만 요구하고, 돌아올 자리를 잃지 않는다                      | `middleware.ts` `PROTECTED_PATHS` 에 없음 · S3 #4                                                                           |
| 2   | **목록이 정본, 지도는 가속기.** 선택 뷰 목록은 완전 목록 출처(정적 25 자치구 · `/regions/**`)에서만 오고, 뷰포트 질의(`/map/**`)는 뱃지 지름길에만 쓴다                                            | [condition-selector D5-2](./condition-selector.md#d5-2-목록-완전성-규칙)                                                    |
| 3   | 상위 조건이 바뀌면 하위를 초기화한다 — 자치구 → 행정동만. **업종은 지역을 바꿔도 유지한다**                                                                                                        | [condition-selector D5-1](./condition-selector.md#d5-1-하위-조건-초기화)                                                    |
| 4   | 조건·결과 단계·고른 상권은 URL 이 나른다(`districtCode`·`administrationCode`·`serviceCode`·`view=results`·`commercialCode`). **이름·점수는 URL 에 싣지 않는다** — 링크가 낡은 값을 들고 되살아난다 | [url-state §1·§3](./url-state.md)                                                                                           |
| 5   | URL 은 **씨앗이자 거울**이다. 마운트 때 한 번 리듀서를 채우고, 그 뒤로는 리듀서가 정본이고 URL 은 `history.replaceState` 로 따라간다. 양방향 동기화를 두지 않는다                                  | [url-state §2](./url-state.md)                                                                                              |
| 6   | 추천 기준 시점은 `periodCode=20233` 으로 고정하고 화면에 「2023년 3분기 기준」을 적는다. 시점 선택 UI 를 두지 않는다                                                                               | `RECOMMENDATION_PERIOD_CODE` · `formatRecommendationPeriod`                                                                 |
| 7   | `topN` 은 백엔드 허용 범위 **5~30** 안에서만 보낸다(벗어나면 400 `COMMERCIAL_101`). 기본 5 이고 화면 문구 「추천 Top 5」와 짝이다                                                                  | `clampRecommendationTopN`                                                                                                   |
| 8   | 결과가 도착하면 **1위를 자동 선택**해 목록·상세가 빈 채로 뜨지 않게 한다. 다만 자동 선택은 카메라를 결과 전체에 맞추고, **사용자가 직접 고른 경우에만** 그 상권으로 좁힌다                         | `resultSelectionSource: 'auto' \| 'user'`                                                                                   |
| 9   | 점수를 색으로 옮길 때는 **지표의 방향**을 먼저 본다. 위험도·혼잡도는 낮을수록 좋다. 모르는 지표 코드는 중립색이고 색으로 판단하지 않는다. 도넛의 호 길이는 점수 그대로, 색만 등급이다              | [ux-followups D-1](./ux-followups.md) · `metric-polarity.ts`                                                                |
| 10  | `compositeScore: null` 은 「지표 데이터가 없어 점수를 낼 수 없다」이다(집계 대기가 아니다). 오류로 다루지 않고 게이지를 그리지 않는다                                                              | `recommend-result-list.tsx`                                                                                                 |
| 11  | 추천 이유(`selectionReason`)는 **자르지 않는다.** 왜 이 순위인지를 말하는 유일한 문장이다                                                                                                          | `recommend-result-list.tsx`                                                                                                 |
| 12  | 비교는 **정확히 두 상권**이다. 계약(`leftCommercialCode`/`rightCommercialCode`)이 두 자리뿐이라 셋째 이후는 담지 못하게 하고, URL 이 셋 이상을 들고 오면 잘랐다는 사실을 화면이 말한다             | [compare-url](../../../src/lib/recommend/compare-url.ts) `COMPARE_MAX_COMMERCIALS`·`truncated`                              |
| 13  | 비교 표는 **판단을 옮기지 않는다.** 응답의 `winnerSide` 를 표에 색으로 칠하지 않고 값과 차이(좌−우, 부호 유지)만 적는다. 승자·추천 이유는 근거가 함께 있는 리포트 영역에서만 말한다                | `comparison-presentation.ts`                                                                                                |
| 14  | 모바일에서는 패널이 곧 바텀시트다. 선택 뷰는 **같은 패널의 세 번째 뷰**로 열어 시트 위에 시트를 겹치지 않는다. 시트 규격은 분석과 공유한다(`lib/map/bottom-sheet-state`)                           | [ux-followups C](./ux-followups.md#c-모바일-바텀시트-통일) · [analysis mobile-responsive](../analysis/mobile-responsive.md) |
| 15  | 지도 SDK 실패·지역 목록 실패·추천 실패·프로필 부분 실패를 구분한다. 프로필이 일부 실패해도 추천 목록은 유지한다. 오류 분류는 `resolveApiError`/`isRetryable` 로만 한다                             | S5 TC-007 · `lib/api/api-error.ts`                                                                                          |
| 16  | 북마크는 회원 북마크 API 의 표현 범위(상권 코드·이름)만 저장한다. 업종·시점까지 저장한다고 오인시키지 않는다. 같은 상권에 대한 중복 토글은 예약(reservation)으로 한 번만 보낸다                    | `recommend-bookmarks.ts` · `recommend-page.tsx` 예약 로직                                                                   |
| 17  | 키보드만으로 세 조건을 모두 고르고 결과를 탐색할 수 있다. 지도 뱃지는 접근성 대체 경로가 아니다. 터치 대상 44px, 포커스 표시는 `DESIGN.md` 토큰                                                    | [condition-selector D6-2](./condition-selector.md#d6-2-지도-뱃지는-접근성-대체-경로가-될-수-없다)                           |
| 18  | 패키지 버전·lockfile·빌드·배포 설정과 백엔드는 바꾸지 않는다                                                                                                                                       | 전체                                                                                                                        |

---

## S3. 필수 기능

| #   | 기능명               | 한 줄 설명                                                                                                                                   | 세부 명세                                                           | 상태      |
| --- | -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------- | --------- |
| 1   | 조건 선택            | 조건 바 한 줄 + 패널 내 선택 뷰 + 지도 뱃지 지름길로 자치구·행정동·업종을 고른다. 12개 넘는 목록은 검색을 준다                               | [condition-selector](./condition-selector.md)                       | 구현 완료 |
| 2   | 추천 결과            | 행정동 상권 전체를 후보로 업종별 Top 5 를 받아 점수 도넛·추천 이유·빈 자리 덤벨·업종 아이콘으로 보여 준다. 1위 자동 선택, 결과 폴리곤·카메라 | [ux-followups A·B·D](./ux-followups.md)                             | 구현 완료 |
| 3   | URL 상태             | 조건·결과 단계·고른 상권을 URL 이 나르고 새로고침·링크 공유에서 복원한다                                                                     | [url-state](./url-state.md)                                         | 구현 완료 |
| 4   | 상권 저장(북마크)    | 결과 카드에서 상권을 회원 북마크로 저장·해제한다. 비로그인은 로그인으로 보내고 돌아온다                                                      | S2 #1·#16 (세부명세 없음 — 이 문서가 정본)                          | 구현 완료 |
| 5   | 상권 비교            | 결과에서 두 상권을 담아 `/recommend/compare` 로 간다. 백엔드 비교 응답을 그룹별 표(값·차이)와 판정 리포트·AI 패널로 보여 준다                | S2 #12·#13 (세부명세 없음 — 이 문서와 코드 주석이 정본)             | 구현 완료 |
| 6   | 비교 초안으로 글쓰기 | 비교 화면에서 커뮤니티 글쓰기를 열면 백엔드 초안(제목·본문·분석 첨부 4필드)으로 채운다. 대상은 **행정동 고정**                               | [community S4 대상 규약](../community/community.md#대상target-규약) | 구현 완료 |
| 7   | 모바일 바텀시트      | 좁은 뷰포트에서 패널을 단일 바텀시트로 통합한다(접힘 72px + 첫 줄 노출, 지도 최소 높이 보장)                                                 | [ux-followups C](./ux-followups.md#c-모바일-바텀시트-통일)          | 구현 완료 |

### S3-1. 추천 호출이 세 단계인 이유

추천 API 는 **후보 상권 코드 목록을 요구**한다(`commercialCodes[]`). 행정동을 넘기면 알아서 골라 주는 계약이 아니다. 그래서 화면은 행정동 상권 목록을 먼저 받아 전부를 후보로 넘기고, 응답 Top 5 각각의 프로필을 다시 받는다. 후보 목록은 `createStableCommercialCodes` 로 중복 제거·정렬해 **같은 조건이면 같은 쿼리 키**가 되게 한다 — 그렇지 않으면 순서만 다른 같은 요청이 캐시를 빗나간다.

홈 03단계 미리보기(`use-recommend-preview`)가 이 연쇄를 그대로 쓴다. 추천 규칙이 바뀌면 홈도 같이 움직여야 한다.

### S3-2. 지도 뱃지가 목록을 대체할 수 없는 이유 (실측)

지도 영역 질의는 뷰포트 기반이라 첫 진입에서 자치구 25개 중 **21개만** 뱃지가 뜬다(강동·관악·금천·도봉 누락). 그래서 뱃지는 지름길이고 선택 뷰 목록이 정본이다. 상권 경계도 `profile` 이 `centerLng: null`/`boundaryCoords: []` 를 주는 경우가 있어, 결과 폴리곤은 결과 중심점 bbox 로 뷰포트 질의해 우회한다 — 근본 해결은 백엔드가 경계를 채우는 것이다(ux-followups D8).

## S4. 세부 명세

- [조건 선택 UX 세부 명세](./condition-selector.md) — 조건 바·선택 뷰·지도 뱃지·검색·`/analysis` 공유 부품
- [URL 상태 세부 명세](./url-state.md) — 파라미터·씨앗과 거울·복원 실패 처리
- [UX 후속 3건 세부 명세](./ux-followups.md) — A 업종 선택 · B 결과 지도 카메라/폴리곤 · C 모바일 바텀시트 · D 보고서 시각화(점수 방향)

북마크·비교·비교 초안은 세부명세가 없다. 규칙은 이 문서 S2 와 코드 주석(`compare-url.ts`·`comparison-presentation.ts`·`recommend-bookmarks.ts`)이 정본이고, 흐름이 커지면 그때 세부명세로 뗀다.

## S5. 테스트케이스

| TC ID  | 범위 | 사전 조건                     | 수행 절차                                                       | 기대 결과                                                                                                                |
| ------ | ---- | ----------------------------- | --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| TC-001 | S    | 비로그인                      | `/recommend` 에서 조건 셋을 고르고 「상권 추천받기」            | 로그인 요구 없이 Top 5 가 뜨고 1위가 선택돼 있다                                                                         |
| TC-002 | S    | 비로그인, 결과 화면           | 결과 카드의 북마크 버튼                                         | 로그인으로 보내고, 로그인 뒤 같은 결과 URL 로 돌아온다                                                                   |
| TC-003 | S    | 결과 URL 보유(`view=results`) | 새 탭에서 열기 · 새로고침                                       | 같은 조건·결과·고른 상권이 복원된다. 이름은 API 로 다시 채운다                                                           |
| TC-004 | S    | 조건 선택 중                  | 자치구를 바꾼다                                                 | 행정동은 비고 업종은 유지된다                                                                                            |
| TC-005 | S    | 첫 진입, 지도 기본 뷰포트     | 지도에 뱃지가 없는 자치구(예: 도봉구)를 선택 뷰 목록에서 고른다 | 목록에 있고 선택된다. 뱃지 유무와 무관하다                                                                               |
| TC-006 | S    | 결과 화면                     | 위험도 90 · 기회도 90 인 두 지표를 본다                         | 호 길이는 둘 다 90 이고, 색은 위험도가 나쁨·기회도가 좋음이다                                                            |
| TC-007 | S    | 결과 화면, 프로필 한 건 실패  | 페이지 진입                                                     | 추천 목록·점수는 유지되고, 실패한 상권의 프로필 부가 정보만 빠진다(성공한 프로필만 모은다 — `collectSuccessfulProfiles`) |
| TC-008 | S    | 결과 화면                     | 상권 셋을 비교에 담으려 한다                                    | 셋째는 담기지 않는다. `commercialCodes` 셋을 URL 로 직접 넣으면 잘렸다는 안내가 뜬다                                     |
| TC-009 | S    | 비교 화면                     | 표를 본다                                                       | 값·차이만 있고 승패 색이 없다. 판정은 리포트 영역에만 있다                                                               |
| TC-010 | S    | 비교 화면                     | 글쓰기(비교 초안)를 누른다                                      | `/community/register` 가 초안 제목·본문으로 채워지고 대상은 행정동이다. 저장 때만 로그인 요구                            |
| TC-011 | S    | 모바일(≤768px)                | 조건 바 조각을 눌러 선택 뷰를 연다                              | 같은 시트 안에서 뷰가 바뀐다. 시트가 겹치지 않고 지도 최소 높이가 유지된다                                               |
| TC-012 | S    | 키보드만                      | 조건 셋 선택 → 추천 → 결과 카드 이동                            | 마우스·지도 뱃지 없이 끝까지 된다                                                                                        |

세부 기능 전용 TC 는 각 세부명세의 D7 에 있다(condition-selector D7 · url-state §7 · ux-followups D7).

---

## 변경 이력

| 버전 | 날짜       | 변경 내용                                                                                                               | 작성자 |
| ---- | ---------- | ----------------------------------------------------------------------------------------------------------------------- | ------ |
| 1.0  | 2026-09-10 | 사후 최초 작성. 세부명세 3종과 코드(`src/components/recommend/**`, `src/lib/recommend/**`)에 흩어진 규칙을 S2 로 모았다 | Claude |
