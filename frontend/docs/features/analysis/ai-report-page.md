[//]: # '저장 경로: docs/features/analysis/ai-report-page.md'

# analysis — AI 리포트 전용 페이지(지표+AI 결합) 세부 명세서

> **작성일**: 2026-08-12
> **공통 명세**: [analysis 공통 명세](./analysis.md) · 관련: [ai-report](./ai-report.md)
> **대상**: 웹 (Next.js App Router)
> **작성자**: Claude Code
> **상태**: 초안 (설계 확정, 구현 계획 전 — 다음 스레드에서 writing-plans로 이어감)

AI 리포트가 지금은 **텍스트 서술만** 있고(사이드바 패널), 실제 지표·차트는 별도 "전체 분석 보기" 결과 페이지에 흩어져 있다. 이 둘을 합쳐 **"AI가 실제 데이터 위에서 정리해 준 리포트"**를 보여주는 **전용 페이지**를 만든다. 데이터는 빨리 오고 AI 서술은 느린 비대칭을 **2단 속도(two-speed) 점진 렌더링**으로 해소한다. **v1은 상권(commercial) 레벨만** 지원한다.

[[_TOC_]]

---

## 다음 스레드 이어가기 (핸드오프)

- 이 문서가 확정 설계다. 다음 단계는 **writing-plans**로 실행 계획 작성 → subagent-driven-development.
- **작업 격리**: 새 worktree 권장(예: `.worktrees/bosspick-ai-report`, `develop` 기준). 현재 홈 작업 worktree(`bosspick-home`)와 분리.
- 이 기능은 **FE 전용**. 백엔드 API 계약 변경 없음(기존 상권 데이터 페처 + 기존 AI 리포트 job/SSE 재사용).
- 구현 시 확정할 열린 항목은 각 섹션의 **⚠️ 구현 시 확정** 표기 참조.

---

## D0. 배경 / 기획 의도

| 항목              | 내용                                                                                                                    |
| ----------------- | ----------------------------------------------------------------------------------------------------------------------- |
| 충족 요구사항     | analysis 공통 명세의 AI 리포트 가치 강화. 기존 [ai-report](./ai-report.md)(텍스트 서술)을 데이터와 결합해 고도화        |
| 해결하려는 문제   | ① AI 리포트가 텍스트뿐이라 "리포트다움"·설득력이 약함 ② 지표·차트는 방대한 결과 페이지에 흩어져 한눈에 안 들어옴        |
| 목표 동작 (to-be) | 상권 선택 → 전용 리포트 페이지: 핵심 지표·엄선 차트(빠름)가 즉시, AI 서술(느림)이 스트리밍으로 채워짐                   |
| 구현 제외 범위    | 지역(자치구/행정동) 레벨(후속). 백엔드 스키마에 점수/수치 필드 추가(백엔드 협의 대상, 이번 제외). 결과 페이지 자체 개편 |
| 연관 세부 기능    | [ai-report](./ai-report.md)(`useAiReport` 재사용), 결과 페이지(`analysis-result-view.tsx`, 데이터 페처·차트 재사용)     |

---

## D1. 기능 개요

```
상권+업종 선택(level=commercial, code, serviceCode)
  ├─ 빠른 층: 상권 데이터 쿼리(fetchCommercial*) → 핵심 지표 카드 + 엄선 차트 3개 즉시 렌더
  └─ 느린 층: useAiReport(SSE job) → AI 요약/강점/주의/추천 스트리밍 채움(도착 전 스켈레톤+진행 스테퍼)
  → 하단 "전체 데이터 분석 보기"로 기존 결과 페이지 위임
```

전용 페이지는 **정독용 목적지**다. 지도 위 **사이드바 AI 패널은 빠른 요약 미리보기로 유지**하고(진입 A), 사이드바의 primary CTA로 이 페이지에 들어온다.

### D1-1. UI 진입점 / 기능 연결

> **라우트/위치(신규)**: `app/(shell)/analysis/report/page.tsx`(가칭 — ⚠️ 구현 시 라우트명 `report` vs `ai-report` 확정). searchParams: `code`(상권코드), `serviceCode`(업종). 기존 `app/(shell)/analysis/result`와 형제.
> **진입**: 사이드바 `AiReportPanel` 푸터/헤더의 CTA "AI 리포트 보기" → 이 페이지. 링크 빌더는 `createAnalysisResultHref`(`src/lib/analysis/selection.ts:163`) 옆에 `createAiReportHref` 신설.
> **깔때기**: 사이드바(훑기) → 전용 리포트(정독) → "전체 데이터 분석 보기"(원본 덤프, 기존 결과 페이지).

| UI 요소                   | 사용자 동작 | 결과 / UI 반영                                                   |
| ------------------------- | ----------- | ---------------------------------------------------------------- |
| 사이드바 "AI 리포트 보기" | 클릭        | 전용 리포트 페이지로 이동(현재 선택 상권·업종 전달)              |
| 핵심 지표 카드            | (표시)      | 데이터 도착 즉시 채움. 도착 전 개별 스켈레톤                     |
| 엄선 차트 3개             | (표시)      | 데이터 도착 즉시 렌더(recharts)                                  |
| AI 인사이트 영역          | (표시)      | 생성 중: 진행 스테퍼+스켈레톤 → 도착 시 요약/강점/주의/추천 채움 |
| "전체 데이터 분석 보기"   | 클릭        | 기존 결과 페이지(`/analysis/result?...`)로 이동                  |
| 비로그인                  | 진입        | AI 영역은 잠금 카드(로그인 CTA). 데이터 차트 노출 여부는 D6 참조 |

---

## D2. 동작 요구사항

| #   | 요구사항                                                                                                  | 상세 참조 |
| --- | --------------------------------------------------------------------------------------------------------- | --------- |
| 1   | 빠른 층(지표·차트)은 상권 데이터 쿼리 도착 즉시 렌더하며 AI 완료를 기다리지 않는다                        | D3, D4    |
| 2   | 느린 층(AI 서술)은 자리(레이아웃)를 먼저 잡고 스켈레톤+진행 스테퍼(job stage/progressMessages)로 표시     | D3, D4    |
| 3   | AI 요약/강점/주의/추천이 도착하면 해당 자리에 채워진다(레이아웃 점프 최소화)                              | D4        |
| 4   | 차트 큐레이션은 v1 3개: 시간대별 매출(Line) · 요일별 유동인구(Bar) · 연령·성별 유동인구(Pyramid)          | D5        |
| 5   | 핵심 지표 카드 4개: 월 매출 · 유동인구 · 점포 수(경쟁) · 매출 변화(성장률)                                | D5        |
| 6   | 강점=긍정색·주의=경고색으로 색상/아이콘 위계를 준다(현 텍스트 나열 대비 개선)                             | D5, D6    |
| 7   | 비로그인 사용자는 AI 영역에 잠금 카드(로그인 CTA)를 노출한다(기존 `AiReportLockCard`·`useAuthStore` 준용) | D6        |
| 8   | 하단에 "전체 데이터 분석 보기" 링크로 기존 결과 페이지로 위임한다                                         | D1-1      |
| 9   | 개별 데이터 쿼리 실패/빈 값은 해당 카드·차트만 빈 상태로 처리하고 나머지는 계속 보인다                    | D4        |
| 10  | 모션·색·radius·shadow는 `DESIGN.md` 토큰만. reduced-motion에서 스켈레톤 shimmer/전환 제거                 | D6        |

---

## D3. 아키텍처 / 시스템 설계

### D3-1. 두 개의 독립 데이터 소스(2단 속도의 근거)

| 층           | 소스                                                                                                                                                                                 | 특성          |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------- |
| 빠른(데이터) | 상권 데이터 REST 쿼리(react-query): `fetchCommercialSalesSummary`, `fetchCommercialFootTraffic`, `fetchCommercialSales`, `fetchCommercialPopulation`, `fetchCommercialTrend`(성장률) | 즉시성 높음   |
| 느린(AI)     | `useAiReport({ level:'commercial', code, serviceCode, active, enabled })` — 제출 POST → SSE job → 폴링 폴백                                                                          | 수 초~수십 초 |

두 소스는 **서로 기다리지 않는다**. 페이지 골격은 즉시 그리고, 각 영역이 자기 소스 상태에 따라 독립적으로 채워진다.

### D3-2. 재사용 / 신규

| 구분   | 대상                                                                                                                                                                                                                                                                                                                                       |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 재사용 | 차트: `LineChart`/`BarChart`/`PopulationPyramid`(`src/components/analysis/charts/*`, `height` prop 지원). 변환: `toTrendPoints`/`toPyramidRows`(`src/lib/analysis/chart-data.ts`). 데이터 페처: `fetchCommercial*`(결과 뷰가 쓰는 것과 동일). AI: `useAiReport`·`ai-report-presentation`(뷰 변환)·`AiReportLockCard`. 인증: `useAuthStore` |
| 신규   | `app/(shell)/analysis/report/page.tsx`(라우트 셸, searchParams 파싱). `ai-report-page-view.tsx`(2단 레이아웃 오케스트레이션). 리포트 프리젠테이션 컴포넌트: 지표 카드 행, 차트 큐레이션 블록, AI 인사이트 블록(강점/주의/추천 색상 카드), 진행 스켈레톤·스테퍼. `createAiReportHref`(selection.ts).                                        |

> **매핑 참조(⚠️ 구현 시 확정)**: 각 상권 페처 응답 → 차트 데이터로의 변환은 이미 `analysis-result-view.tsx`(대략 L1289~1500: 시간대별 매출 Line, 요일별 유동인구 Bar, 연령·성별 유동인구 Pyramid)에서 수행한다. 그 매핑을 추출/공유(작은 셀렉터 함수)해 리포트와 결과 뷰가 같은 변환을 쓰게 한다 — 로직 중복 금지.

### D3-3. 클라이언트 경계

- 페이지 셸(`page.tsx`)은 서버 컴포넌트로 searchParams만 읽고, 데이터·AI·차트가 필요한 뷰 본체는 `'use client'`.
- SSR 초기엔 골격(헤더/섹션 틀 + 스켈레톤)만 렌더, 클라이언트에서 쿼리·SSE 채움.

---

## D4. 상호작용 / 상태 설계

| 영역        | 상태 소유                              | 로딩 표현                                                                                                  |
| ----------- | -------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| 지표 카드   | 각 상권 쿼리(react-query)              | 카드별 스켈레톤 → 값                                                                                       |
| 차트 3개    | 각 상권 쿼리 + `hasLineData` 등 빈검사 | 차트별 스켈레톤/"데이터 없음"                                                                              |
| AI 인사이트 | `useAiReport` → `AiReportState`        | loading(stage/progressMessages)→스테퍼+스켈레톤; ready-commercial→요약/강점/주의/추천; error→재시도; empty |

- 부분 실패 격리: 한 쿼리 실패가 페이지 전체를 막지 않는다(D2-9).
- AI 재시도: 기존 `useAiReport` `retry()` 노출.
- 레이아웃 점프 최소화: AI 블록은 도착 전에도 최소 높이 확보(스켈레톤이 최종 형태 근사).

---

## D5. 데이터 / 콘텐츠 (큐레이션 확정)

**헤더**: `상권명 · 업종명` + AI 한 줄 종합(`headline.summary`, 도착 전 스켈레톤) + "데이터 준비됨" 배지.

**핵심 지표 카드(4, 빠름)**:

| 카드              | 소스                                    |
| ----------------- | --------------------------------------- |
| 월 매출           | `fetchCommercialSalesSummary`           |
| 유동인구          | `fetchCommercialFootTraffic`(요약치)    |
| 점포 수(경쟁)     | `fetchCommercialStores`/summary         |
| 매출 변화(성장률) | `fetchCommercialTrend`(sales) 방향/증감 |

**차트 큐레이션(3, 빠름)**:

| 차트               | 컴포넌트            | 의미          |
| ------------------ | ------------------- | ------------- |
| 시간대별 매출      | `LineChart`         | "언제 파나"   |
| 요일별 유동인구    | `BarChart`          | "언제 붐비나" |
| 연령·성별 유동인구 | `PopulationPyramid` | "누가 오나"   |

**AI 인사이트(느림, 스트리밍)** — `toCommercialReportView` 뷰 사용:

- 종합 요약(`headline.summary`) + 인사이트(`headline.insight`)
- **강점**(`strengths[]`, 긍정색 카드) / **주의**(`risks[]`, 경고색 카드)
- 추천 업종군 · 고객층 · 영업시간(`actions[]`, 칩)

**푸터**: "전체 데이터 분석 보기" → `/analysis/result?...`.

> 카피 톤: 예비 창업자 대상 한국어. 수치는 대표 예시가 아니라 **실데이터**(사이드바 미니데모의 "대표 예시"와 구분). `DESIGN.md` 준수.

---

## D6. 접근성 / 비기능 요구

| 항목           | 요구                                                                                                                                         |
| -------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| 인증           | 비로그인 → AI 영역 잠금 카드(로그인 CTA, returnUrl 포함). 데이터 차트 노출 정책은 ⚠️ 구현 시 확정(기존 사이드바는 로그인 사용자만 패널 오픈) |
| reduced-motion | 스켈레톤 shimmer·진행 전환 제거, 최종/정지 상태 표시                                                                                         |
| 스크린리더     | AI 생성 진행은 `aria-live=polite`. 차트는 `role=img`+aria-label(기존 차트 컴포넌트 준수)                                                     |
| 디자인 토큰    | 색·모션·radius·shadow 전부 `DESIGN.md` 토큰. 강점/주의 색은 positive/warning 계열                                                            |
| 반응형         | 데스크톱: 지표 4열 + 차트 카드. 모바일: 단열 스택                                                                                            |

---

## D7. 성능

- 빠른 층 쿼리는 병렬(react-query), 각자 도착 즉시 렌더. AI SSE는 기존 훅 그대로(추가 연결 없음).
- 차트는 활성 영역만. `height` prop로 리포트 맥락에 맞게 축소.
- 스켈레톤으로 CLS(레이아웃 점프) 억제.

## D8. 테스트

- 큐레이션 셀렉터(페처 응답→차트 데이터) 순수 변환 단위테스트(빈/경계).
- `createAiReportHref` 단위테스트(selection.ts 패턴).
- 페이지 뷰 SSR 렌더: 골격·섹션 제목·스켈레톤 존재.
- 2단 상태 렌더 테스트: 데이터 도착+AI loading 동시 상태에서 "차트는 있고 AI는 스켈레톤".
- 인증 분기: 비로그인 시 AI 영역 잠금 카드.
- reduced-motion 분기(전환 제거).

---

## 열린 결정 (구현 시 확정됨)

1. ✅ 라우트명 `analysis/report` 확정.
2. ✅ 비로그인 = **데이터 차트 노출 + AI 영역만 잠금**.
3. ✅ 필드→표시 매핑을 공유 셀렉터 `commercial-chart-selectors.ts`로 추출(결과 뷰·리포트 공유).
4. ✅ "성장률" = `fetchCommercialTrend({metricType:'SALES'})`의 `trendDirection` + 최신 분기 `changeRate`.

---

## D9. 확장: 사이드바 인라인 리포트 + 크게보기 모달 (공유 본문)

> **작성일**: 2026-08-12 (확장) · 상태: 설계 확정, 구현 전 → writing-plans

### D9-1. 배경 / 문제

전용 페이지(`/analysis/report`)는 정독용으로 좋으나 **페이지 이동**을 강제한다. 지도 맥락에서 상권을 훑는 사용자는 사이드바를 떠나지 않고 리포트를 보고 싶다. 또한 현재 전용 페이지는 **본문 배경과 카드가 모두 회색**(`--color-surface-muted`)이라 콘텐츠 구분이 약하다.

### D9-2. 목표 동작 (to-be)

- 지도 사이드바 AI 패널이 **전체 리포트(지표 4카드 + 엄선 차트 3개 + AI 인사이트)를 세로 1열**로 인라인 표시.
- 패널 헤더의 **"크게보기"** 버튼 → **모달**로 같은 리포트를 크게(넓은 레이아웃).
- 전용 페이지는 **즐겨찾기/공유 URL용으로 유지**.
- 배경 대비 개선: 리포트 본문 배경 = 흰색.

### D9-3. 아키텍처: 공유 본문 컴포넌트

- 현재 `ai-report-page-view.tsx`의 본문(4개 fast 쿼리 + `useAiReport` + 지표/차트/인사이트 조립)을 **`ai-report-body.tsx`(`AiReportBody`)** 로 추출.
  - Props: `{ selection: AnalysisSelection; variant: 'compact' | 'full' }`.
  - 데이터·AI를 **자체 소유**. react-query 키(`['analysis',...]`, `['ai-report','submit',...]`)가 dedupe하므로, 페이지·사이드바·모달이 동시에 없거나 동일 selection이어도 **중복 제출 없음**.
- 세 표면이 **동일 본문** 재사용:
  | 표면 | variant | 래퍼 |
  | ---- | ------- | ---- |
  | 전용 페이지 `/analysis/report` | `full` | `ai-report-page-view.tsx`(헤더+본문+푸터, Main 흰 배경) |
  | 사이드바 패널 | `compact` | `ai-report-panel.tsx`(헤더[크게보기]+본문+푸터) |
  | 크게보기 모달 | `full` | `ui/dialog.tsx` 재사용 + 본문 |

### D9-4. variant 차이 (같은 데이터, 크기만)

| | `compact`(사이드바) | `full`(모달·페이지) |
| --- | --- | --- |
| 지표 카드 | 1열 스택(또는 2열) | 2~4열 |
| 차트 높이(`height` prop) | 축소(예 160) | 200 |
| 여백/폭 | 좁게 | 넉넉 |

### D9-5. 사이드바 패널 변경

- 헤더에 **"크게보기"** 버튼(확대 아이콘) → 모달 오픈. 기존 **"AI 리포트 보기"(페이지 이동 CTA)는 제거하고 "크게보기"로 대체**.
- 본문 = `<AiReportBody selection variant="compact" />` (기존 `CommercialReportBlocks` 텍스트-only 본문 대체).
- 푸터 **"전체 분석 보기"**(→ `/analysis/result`)는 유지.
- `analysis-page.tsx`가 패널에 **`selection`을 전달**(현재 `state`만 전달 → 본문이 자체 쿼리하도록 selection 필요). 기존 `useAiReport`/카드-프리뷰 로직과 충돌 없이(키 dedupe).

### D9-6. 크게보기 모달

- `ui/dialog.tsx` 재사용. 내용 = `<AiReportBody selection variant="full" />`.
- 데스크톱: 중앙 큰 다이얼로그. 모바일: 전체화면 시트.
- 접근성: `role="dialog"`, ESC/오버레이 닫기, 포커스 트랩(다이얼로그 컴포넌트 준수), 열기 버튼으로 포커스 복귀.

### D9-7. 비로그인 & 배경

- 비로그인: `AiReportBody` 내부 `resolveInsightMode`가 처리 → **차트·지표 노출 + AI만 잠금**(D6 정책과 통일). 사이드바도 동일(기존 "잠금 카드만" → 본문 표시로 통일).
- 배경: `AiReportBody`/페이지 `Main`/모달 본문 배경 = **`var(--color-surface)`(흰색)**. 지표 카드·차트 카드는 `--color-surface-muted` 유지 → 대비 확보. 임의 색 추가 없음(`DESIGN.md` 토큰).

### D9-8. 반응형 / 성능

- 사이드바(좁음)에 차트 3개 세로 = 스크롤 길어짐 → 패널 자체 스크롤 허용, 차트 `height` 축소로 완화.
- 쿼리는 selection 단위로 캐시 공유(페이지↔사이드바↔모달 재요청 없음).

### D9-9. 테스트

- `AiReportBody` variant 분기(순수 로직이 있으면 리졸버로, 렌더는 기존 관행상 소스-계약/순수 리졸버 테스트).
- 모달 열림/닫힘 상태 로직(순수 훅/상태), "크게보기" 버튼 존재(source-contract).
- 배경 토큰(`--color-surface`) 적용 확인(source-contract 또는 스냅 대체 금지—토큰 문자열 확인).
- 회귀: 전용 페이지·사이드바·모달이 동일 `AiReportBody`를 렌더하는지(중복 로직 없음).

### D9-10. 재사용 / 신규

| 구분 | 대상 |
| ---- | ---- |
| 재사용 | `AiReportPageView`의 쿼리/리졸버/섹션 컴포넌트 전부, `ui/dialog.tsx`, `report-{metric-cards,chart-section,insight-section}`, 차트 `height` prop |
| 신규 | `ai-report-body.tsx`(`AiReportBody`, variant), 크게보기 모달 래퍼(패널 내 상태), "크게보기" 버튼 |
| 수정 | `ai-report-page-view.tsx`(본문→AiReportBody 위임 + Main 흰 배경), `ai-report-panel.tsx`(본문 교체 + 크게보기 버튼, selection prop), `analysis-page.tsx`(패널에 selection 전달) |

### D9-11. 열린 결정 (D9)

- 모두 확정: (a) "AI 리포트 보기" → "크게보기"(모달) **대체**, (b) 비로그인 사이드바 **차트 노출+AI 잠금 통일**, (c) compact 사이드바에 **차트 포함**, (d) 본문 배경 **흰색**.

---

## 변경 이력

| 날짜       | 내용                                                                  |
| ---------- | --------------------------------------------------------------------- |
| 2026-08-12 | 초안: 전용 AI 리포트 페이지(상권 v1), 2단 속도, 진입 A, 큐레이션 확정 |
| 2026-08-12 | 구현 완료(PR #110). 열린 결정 1~4 확정 반영                            |
| 2026-08-12 | D9 확장 설계: 공유 본문(`AiReportBody`) + 사이드바 인라인 + 크게보기 모달 + 흰 배경 |
